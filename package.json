{
  "name": "spacepoint",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Build 3D figures inside a sphere of pyramids using phone orientation + a reach slider.",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "build:standalone": "SINGLEFILE=1 vite build && cp dist/index.html docs/index.html",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^1.2.0",
    "vite": "^5.4.10",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
